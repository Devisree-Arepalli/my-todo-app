{
  "name": "my-todo-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
   "dependencies": {
    "next": "^14.0.0",        // Ensure a specific version is set
    "react": "^18.0.0",       // Ensure a specific version is set
    "react-dom": "^18.0.0"    // Ensure a specific version is set
  }
}
